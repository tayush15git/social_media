<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "SCHEDULE_X" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
  <nav class="navbar">
    <div class="nav-left">
      <%= link_to "SCHEDULE_X", root_path, class: "nav-logo" %>
      
      <% if current_user %>
        <%= link_to "Tweets", tweets_path, class: "nav-link" %>
        <%= link_to "Create a Tweet", new_tweet_path, class: "nav-link" %>
        <%= link_to "Edit a Tweet", "#", class: "nav-link" %>
      <% end %>
    </div>
    <div class="nav-right">
      <% if current_user %>
        <%= link_to "Logout", logout_path, method: :delete, class: "nav-button logout" %>
      <% else %>
        <%= link_to "Sign in with Twitter", "/auth/twitter", class: "nav-button login" %>
      <% end %>
    </div>
  </nav>

  <div class="container">
    <%= yield %>
  </div>

</body>
</html>
